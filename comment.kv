<Comment>
    MDBoxLayout:
        pos: root.pos
        size: self.parent.size
        md_bg_color: .9, .9, .9, 1
        padding: dp(10)
        spacing: dp(10)
        Label:
            id: author_login
            color: (0,0,0,1)
            size_hint: .4, .2
            font_size: dp(24)

        Label:
            id: text
            color: (0,0,0,1)
            font_size: dp(24)
            multiline: True
            size_hint: 1, None
            height: self.texture_size[1]
            text_size: self.texture_size