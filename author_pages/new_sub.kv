<NewSubScreen>:
    MDBoxLayout:
        spacing: dp(10)
        padding: dp(10)
        orientation: 'vertical'
        MDBoxLayout:
            size_hint: 1, .1
            spacing: dp(30)
            MDRoundFlatButton:
                size_hint: .2, 1
                text: 'Back'
                font_size: dp(20)
                on_release:
                    root.go_back(self)
                    app.root.remove_widget(app.root.get_screen('new_sub'))
            MDLabel:
                text: 'New sub'
                font_size: dp(40)
                size_hint: .8, 1

        MDLabel:
            text: "Name"
            text_size: self.size
            font_size: dp(20)
            size_hint: 1, .1
        MDTextField:
            id: name
            mode: 'round'
            font_size: dp(25)
            size_hint: 1, .15

        MDLabel:
            text: "Description"
            text_size: self.size
            font_size: dp(20)
            size_hint: 1, .1
        MDTextField:
            id: description
            mode: 'round'
            multiline: True
            size_hint: 1, .3

        MDLabel:
            text: "Price"
            text_size: self.size
            font_size: dp(20)
            size_hint: 1, .1
        MDTextField:
            id: price
            mode: 'round'
            size_hint: 1, .2

        MDRoundFlatButton:
            text: 'Post'
            font_size: dp(40)
            size_hint: 1, 0.2
            on_release:
                root.publish()
                root.go_back(self)
                app.root.current_screen.load_subs()
                app.root.remove_widget(app.root.get_screen('new_sub'))